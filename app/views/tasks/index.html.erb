<p style="color: green"><%= notice %></p>
<h1 class = "container"><strong>Tasks</strong></h1>
<h5 class = "container"> <strong> Namaskar 🙏🏻🙏🏻🙏🏻🙏 Mr.  <%= current_user.name%></strong></h5>
<div id="tasks">
  <% @tasks.each do |task| %>
    <%if task.completed %>
    <%else%>
      <%= render task %>
    <%end%>
  <% end %>
</div>
<div id="tasks">
  <% @tasks.each do |task| %>
    <%if task.completed%>
      <%= render task %>
    <%end%>
  <% end %>
</div>
<div class = "d-flex justify-content-center">
  <%= link_to "Add task", new_task_path, class: "btn btn-primary nav-font"%>
</div>
